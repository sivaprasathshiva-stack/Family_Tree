# Vercel Postgres connection string (auto-set when you add Vercel Postgres to your project)
POSTGRES_URL=
POSTGRES_PRISMA_URL=
POSTGRES_URL_NO_SSL=
POSTGRES_URL_NON_POOLING=
POSTGRES_USER=
POSTGRES_HOST=
POSTGRES_PASSWORD=
POSTGRES_DATABASE=

# Set to "true" in production to use the API instead of localStorage
VITE_USE_API=true
